import SettingsView from '@/components/screens/SettingsView';

export const metadata = {
  title: 'Settings - ASAS CRM',
  description: 'Manage users and sales pipeline configurations.',
};

export default function SettingsPage() {
  return <SettingsView />;
}
